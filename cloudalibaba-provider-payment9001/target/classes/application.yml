server:
  port: 9001

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #配置nacos地址

#暴露监控端点
management:
  endpoints:
    web:
      exposure:
        include: "*"